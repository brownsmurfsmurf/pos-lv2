# AI駆動開発カリキュラム: 要求からコード生成までをやり直す

対象: Lv2 簡易POSアプリ（要求一覧 Lv1・Lv2 → 要件定義 → 設計 → テスト仕様 → コード）
目的: 「人がやるべきこと」を工程ごとに明確にしたうえで、同じ題材を 1 工程ずつやり直し、
**どういう AI 駆動開発を行ったかを自分の言葉で説明できる**状態にする。

---

## 0. このカリキュラムの前提

### 0.1 1 回目で起きたこと（やり直す理由）

| 講師の指摘 | 何が起きていたか | 人が握っていなかったもの |
|---|---|---|
| 要求に入れていない機能（ログアウト、管理画面、現金決済など）が仕様に入った | AI が「一般的な POS・ログインはこういうもの」という既成概念で埋めた。人はそれを見て「妥当」と受け入れた | **スコープの決定**（何を作らないか） |
| 取引日（JST）をどこで生成するか未定義 | 値引きの期間判定に「今日」が必要なのに、誰も「今日とは何か」を問いにしなかった | **イシューの発見**（決めなければ動かない論点の洗い出し） |

どちらも AI の能力不足ではなく、**「決めるべきこと」を人が言葉にしなかった**ことが原因。
AI は空白を埋めようとするので、空白を放置すると既成概念で埋まる。

### 0.2 役割の原則

```
人  : イシューを定める → 選択肢から決める → 成果物を受け入れる（責任を持つ）
AI  : 選択肢を出す → 成果物を作る → 検証の材料を出す
```

| 分類 | 人がやる（AI に渡さない） | AI に任せる |
|---|---|---|
| イシュー | 「何を決めれば次に進めるか」を列挙する。決めないと動かない論点を見つける | 論点の候補を洗い出す。見落としを指摘する |
| 決定 | 選択肢から選ぶ。選んだ理由を書く。決めないものは「保留」と明示する | 選択肢と、それぞれの影響・トレードオフを示す |
| スコープ | 作るもの／作らないものの線を引く | 要求文から「書いてあること」「書いてないこと」を分類する |
| 成果物 | 受け入れ基準を先に決める。受け入れるか差し戻すかを判断する | 成果物を書く。基準に照らして自己チェックする |
| 検証 | 期待値を決める（何が正しいか） | テストコードを書く。実行して結果を報告する |
| 説明責任 | 講師・採点者に「なぜこうしたか」を説明する | 説明の下書きと根拠の一覧を出す |

**判断のルール**: 「AI が出したから」は理由にならない。すべての決定に「人が選んだ理由」が 1 行つく。

### 0.3 各工程で回す 1 サイクル

```
① イシューを書く（人）      … この工程で決めるべき論点を箇条書きにする
② 選択肢を出させる（AI）    … 論点ごとに 2〜3 案と影響
③ 決める（人）              … 決定ログに「決定 / 理由 / 保留」を記す
④ 作らせる（AI）            … 決定を渡して成果物を生成
⑤ 検証する（人 + AI）       … 受け入れ基準に照らす。AI に別視点のレビューもさせる
⑥ 言語化する（人）          … 「この工程で何を AI に任せ、何を自分が決めたか」を 1 枚に書く
```

⑥ が最終成果物。工程ごとに 1 枚、計 5 枚の「AI 駆動開発の記録」を残す。

### 0.4 成果物の置き場所

```
ai-driven/
├─ 00_決定ログ.md            全工程の決定を通し番号で（D-001, D-002, …）
├─ 01_要件定義/  イシュー.md / 要件定義書_v2.md / 記録.md
├─ 02_設計/      イシュー.md / 設計仕様書_v2.md / 記録.md
├─ 03_テスト/    イシュー.md / テスト仕様書_v2.md / 記録.md
├─ 04_コード/    イシュー.md / 受入チェック.md / 記録.md
└─ 05_振り返り.md            5 枚の記録を束ねた全体の言語化
```

1 回目の成果物（`*_要求忠実版.md`、`pos-app/`）は残す。v2 との差分が学びになる。

---

## 1. 記録テンプレート（毎工程で使う）

### 1.1 イシュー.md

```markdown
# 工程名 — イシュー
## この工程のゴール（1 文）
## 決めなければ次に進めない論点
| # | 論点 | なぜ決める必要があるか | 決めないとどう埋まるか（AI の既成概念） |
## 決めないと明示するもの（保留）
| # | 論点 | 保留する理由 | 誰に確認するか |
## 受け入れ基準（成果物を受け取る条件）
- [ ] …
```

### 1.2 決定ログ（00_決定ログ.md）

```markdown
| ID | 工程 | 論点 | 選択肢 | 決定 | 理由（人の言葉） | 根拠となる要求 |
| D-001 | 要件 | 同一商品の再登録 | a) 行追加 b) 数量加算 | b | Lv2 2.1 が Lv1 2.3 を上書きしている | Lv2 2.1 |
```

### 1.3 記録.md（AI 駆動開発の言語化・1 工程 1 枚）

```markdown
# 工程名 — AI 駆動開発の記録
## 1. 私が定めたイシュー          … 何を決めるべきだと判断したか
## 2. AI に任せたこと             … 指示（プロンプトの要点）と、なぜ任せてよいと判断したか
## 3. 私が決めたこと              … 決定ログの ID と理由
## 4. AI の出力をどう検証したか    … 受け入れ基準、差し戻した点、AI に別視点で見せた点
## 5. 受け入れなかったもの        … AI が提案したが採らなかったものと理由
## 6. 1 回目との違い              … 前回は何を AI に丸投げしていたか
## 7. 次の工程に渡す前提          … 保留のまま渡すもの
```

---

## 2. 工程別カリキュラム

各工程は「人の作業 → AI への依頼 → 人の判断 → 検証 → 言語化」の順。所要時間は目安。

### 工程 0: 準備（約 1 時間）

**人がやること**
1. 要求一覧（Lv1・Lv2）を自分で読み、「書いてあること」に下線、「書いていないが実装には必要なこと」に印をつける。AI に読ませる前に自分で読む
2. 採点者が何を見るかを 1 文で書く（例: 要求との対応がたどれるか。要求外の機能が混ざっていないか）
3. `00_決定ログ.md` を作り、最初の決定を書く: **D-000「要求に書いてないものは作らない。必要なら保留として明示する」**

**AI に任せること**
- 要求一覧を項番ごとに分解し、「機能」「非機能」「前提」「相互に矛盾する箇所」に分類させる

**AI への依頼例**
> 添付の Lv1・Lv2 要求一覧を項番ごとに分解して、機能／非機能／前提／矛盾の 4 つに分類してください。分類だけを行い、要件の追加や解釈はしないでください。矛盾は「どの項番とどの項番が」と示してください。

**受け入れ基準**: 分類表の各行が要求一覧の項番を 1 つ以上持つ。項番のない行が 1 つでもあれば差し戻す。

**言語化の観点**: 「AI に読ませる前に自分が読んだか」「採点者の視点を先に決めたか」

---

### 工程 1: 要求 → 要件定義（約 3 時間）

**この工程のイシュー（人が書く。例）**
- 同一商品の再登録は行を増やすか数量を増やすか（Lv1 2.3 と Lv2 2.1 が矛盾）
- 会員IDはいつ読み込めるか（Lv1 2.6 と Lv2 2.8 が矛盾）
- Lv2 前提条件の「Lv3 でカメラ」と Lv2 2.1 のカメラ要求の関係
- 「レジ担当者の情報」とは何を表示するのか
- 要求にない次の事項は要件にしない: ログアウト、管理画面、権限、決済方法、キャンセル、返品
- 決めないと動かないが要求にないもの（数量 99 超、空リストの購入、端数、値引きの重複、存在しない会員の後の操作）は**保留 Q-n として一覧にする**

**人がやること**
1. 上のイシュー表を自分で書く。AI に「論点の候補」を出させてよいが、**採用する論点は自分で選ぶ**
2. 決定ログに D-001〜 を書く（矛盾はどちらを採るか、スコープ外は何か）
3. 受け入れ基準を書く: 「全要件に出所の項番がある」「要求一覧の全項番が要件に対応している（対応表）」「要求外の機能が 0 件」「保留 Q が一覧化されている」

**AI に任せること**
- 決定ログと分類表を渡して要件定義書の本文を書かせる
- 出来上がったら別の役（採点者役）で読ませ、「要求にない要件」「出所のない要件」を指摘させる

**AI への依頼例**
> 決定ログ D-000〜D-0xx と要求分類表に基づいて要件定義書を書いてください。要件は 1 件ずつ番号（FR-xx-x）と出所（要求一覧の項番）を付けてください。決定ログにない解釈が必要になったら、要件にせず「確認事項 Q-n」として末尾に列挙してください。
>
> （検証時）あなたは採点者です。この要件定義書の各要件について、出所の項番に本当にその内容が書かれているか照合し、書かれていない要件を列挙してください。

**受け入れ（人）**: 対応表を自分の目で 1 行ずつ確認する。AI のレビュー結果を「採用／不採用」に分け、理由を書く。

**言語化の観点**: 「矛盾を誰が見つけたか」「スコープ外を誰が決めたか」「保留を保留のまま残す判断を誰がしたか」

**1 回目との比較で書くこと**: 1 回目は doc-coauthoring の候補を「妥当か」で選んでいた。「要求にあるか」で選んでいなかった。

---

### 工程 2: 要件 → 設計仕様（約 4 時間）

**この工程のイシュー（人が書く。例）**
- **「取引日」の定義と生成場所**: 値引きの期間判定に使う「今日」は、どの層が、どのタイムゾーンで、いつ作るか
- 金額計算の正本はどこか（Frontend か Backend か、両方か）
- Frontend の値と Backend の再計算が違ったらどうするか
- 課題で指定された項目（UML 4 種、ER、上下限、エラー処理、API 一覧、セキュリティ 8 項目）を、**要求の範囲内でどう満たすか**
- 要求にない値（商品コードの桁数、パスワード長、明細行数上限）は「仮置き ★」として Q と結びつける

**人がやること**
1. イシュー表を書く。特に「決めないとコードが書けない値・場所」を全部挙げる（ここを漏らすと取引日の指摘が再発する）
2. 決定ログに設計上の決定を書く（例: D-020「金額計算の正本は Backend の PricingService。Frontend は表示のみ」、D-021「取引日は Backend が UTC 現在時刻を Asia/Tokyo に変換して 1 回だけ作る」）
3. 受け入れ基準: 「全 API に入力の型・出力・エラーがある」「全テーブル列に対応する要件番号がある」「仮置き ★ の全件が Q に紐づく」「課題の指定項目 → 章の対応表がある」「**日時・タイムゾーン・端数・上限の全てに生成場所と根拠がある**」

**AI に任せること**
- 決定ログを渡して設計仕様書と図（Mermaid）を書かせる
- 「この設計で実装するとき、まだ決まっていない値・場所はどこか」を列挙させる（未決の洗い出し）
- ライブラリのバージョンと既知脆弱性の調査（人は結果の日付と出典を確認する）

**AI への依頼例**
> 要件定義書 v2 と決定ログに基づき設計仕様書を書いてください。要求にない値を決める必要があるときは ★ を付け、要件定義書の Q-n を必ず参照してください。日付・時刻・タイムゾーン・端数処理・上限値は、「どの層が」「どの時点で」「何を基準に」決めるかを明記してください。
>
> （検証時）この設計仕様書だけを渡されて実装する開発者として、実装中に「決まっていない」と手が止まる箇所を全て列挙してください。

**受け入れ（人）**: AI の「手が止まる箇所」一覧を見て、決定ログに追加するか Q に落とすかを 1 件ずつ決める。

**言語化の観点**: 「未決を誰が発見したか（自分のイシュー表か、AI の指摘か）」「★ を Q に結びつける判断を誰がしたか」

**1 回目との比較で書くこと**: 取引日の生成場所は講師に指摘されて初めて定義した。イシュー表に「日時の生成場所」があれば自分で見つけられた。

---

### 工程 3: 設計 → テスト仕様（約 3 時間）

**この工程のイシュー（人が書く。例）**
- **期待値は誰が決めるか**（人）。AI に期待値を決めさせない
- V 字モデルで、要求／要件／設計／実装のそれぞれをどのテストレベルで確かめるか
- 境界値はどこか（数量 1/99、UTC 14:59:59/15:00:00、税率 0/100、値引き期間の開始日・終了日）
- 保留 Q に関わるケースをどう扱うか（★ 仮置きで実施し記録する／実施しない）
- 合格基準（カバレッジの数値、全件合格の範囲）

**人がやること**
1. 代表ケースの**期待値を自分で計算して書く**（例: 150 円 × 2、20% 引き、税率 10% → 小計 240、税額 24、税込 264）。この数値を AI に渡す
2. 境界値の一覧を自分で作る。AI に候補を出させてよいが、採用は自分
3. 受け入れ基準: 「全要件（FR/N）が UT/IT/ST/UAT のいずれかに対応（対応表）」「境界値は前後の値がペアになっている」「期待値のない自動テストがない」「Q に依存するケースが保留と明示されている」

**AI に任せること**
- テスト仕様書の本文・表の生成、ケース番号の採番
- 人が決めた期待値からテストコードの雛形を書く
- 「この境界値の組で漏れている組み合わせ」の指摘

**AI への依頼例**
> 設計仕様書 v2 に基づき、V 字モデルで要求／要件／設計／実装と UAT／ST／IT／UT を対応させたテスト仕様書を書いてください。期待値は私が渡した「期待値表」のみを使い、そこにないケースは期待値欄を「未決（人が決める）」としてください。

**受け入れ（人）**: 期待値表の数値を電卓で再計算する。1 件でも AI が期待値を勝手に埋めていれば差し戻す。

**言語化の観点**: 「期待値を自分で決めたか」「合格基準を先に決めたか」

**1 回目との比較で書くこと**: 1 回目は AI が期待値まで書いた。人は結果表を見ただけで、数値を自分で検算していない。

---

### 工程 4: 設計・テスト → コード生成（約 4 時間）

**この工程のイシュー（人が書く。例）**
- 生成の単位と順序（何から作らせるか。例: 共有定数 → Backend の計算の正本 → API → BFF → 画面）
- **受け入れの手段**: テストが通れば受け入れるのか、加えて人が何を見るのか（例: 取引日の生成コードを目で読む、照合の 409 を実際に起こす）
- 仕様書の ★ をコードでどう扱うか（そのまま実装／実装しない）
- 採用ライブラリのバージョンを仕様書どおりにするか、最新にするか
- 秘密情報（JWT 鍵、DB 接続文字列）の置き場所

**人がやること**
1. 受入チェック表を先に書く（例: 「pytest 全件合格」「UT-B-30/31 の JST 境界が通る」「BFF 経由で 409 が返り DB に行が増えない」「.env とビルド成果物がコミットされていない」）
2. 生成後、**自分で読む箇所を決めて読む**: PricingService の計算、取引日の生成、TransactionService の照合と rollback、BFF の Cookie 設定。全部読まなくてよいが「読んだ箇所」を記録する
3. 実機で 1 回、自分の手で会計を通す（ログイン → スキャン or 手入力 → 会員 → 購入 → ポップアップ → 空に戻る）

**AI に任せること**
- コードの生成、テストの実行、失敗の修正、ビルド、スモークテスト
- 「仕様書のどの番号を実装したか」のコメント付け

**AI への依頼例**
> 設計仕様書 v2 とテスト仕様書 v2 をコンテキストにコードを生成してください。仕様書の番号（API-xx、FR-xx、UT-xx）をコードのコメントで対応づけてください。仕様書に書かれていないことは実装せず、必要なら「実装時に決めた事項」として一覧にして報告してください。テストは実行し、結果をそのまま報告してください。

**受け入れ（人）**: 受入チェック表に自分で ✔ を入れる。AI の「テストは通りました」は ✔ の材料であって ✔ そのものではない。

**言語化の観点**: 「受け入れ基準を生成前に決めたか」「どこを自分の目で読んだか」「AI の報告をどう確かめたか」

**1 回目との比較で書くこと**: 受入基準は AI が事後に示した検証結果をそのまま採用した。人が読んだコードはなかった。

---

### 工程 5: 振り返りと全体の言語化（約 2 時間）

**人がやること**
1. 工程 1〜4 の `記録.md` を並べ、次の問いに答える `05_振り返り.md` を書く
   - 各工程で「イシューを先に書いた」ことで、1 回目と何が変わったか
   - AI に任せてよかった作業／任せてはいけなかった判断
   - 講師の 2 つの指摘は、v2 の手順ならどの工程のどの行為で防げたか
   - 次にゼロから始めるなら、最初の 1 時間で何をするか
2. 決定ログを通読し、「理由」が空の行がないか確認する
3. 講師・採点者への説明を 5 分で話せる形にまとめる（決定ログと対応表を根拠に）

**AI に任せること**
- 記録 5 枚の要約、決定ログの矛盾チェック、説明の下書き

**成果物**: `05_振り返り.md`、Slack 用の要約（任意）

---

## 3. 進め方（セッション計画）

| 回 | 工程 | 人が事前に用意するもの | セッションの終わり |
|---|---|---|---|
| 1 | 工程 0 + 工程 1 のイシュー | 自分で読んだ要求一覧の下線・印、採点者の視点 1 文 | 決定ログ D-000〜、工程 1 のイシュー.md |
| 2 | 工程 1 | 決定ログ、受け入れ基準 | 要件定義書 v2、記録 1 |
| 3 | 工程 2 のイシュー + 設計 | 「決めないとコードが書けない値・場所」の一覧 | 設計仕様書 v2、記録 2 |
| 4 | 工程 3 | 自分で計算した期待値表、境界値一覧 | テスト仕様書 v2、記録 3 |
| 5 | 工程 4 | 受入チェック表 | コード v2、受入チェック ✔、記録 4 |
| 6 | 工程 5 | 記録 1〜4 | 振り返り、説明の原稿 |

各回の冒頭で「今日決めること」を人が読み上げ、終わりに「今日 AI に任せたこと／自分が決めたこと」を 3 行で書く。

---

## 4. 各工程の完了条件（チェックリスト）

- [ ] イシュー.md がある。論点に「決めないとどう埋まるか」が書いてある
- [ ] 決定ログの各行に「理由（人の言葉）」がある
- [ ] 受け入れ基準が成果物の生成**前**に書かれている
- [ ] AI のレビュー結果を「採用／不採用」に分け、不採用の理由を書いた
- [ ] 記録.md の 7 項目が埋まっている
- [ ] 成果物に、AI が勝手に埋めた既成概念（要求にも決定ログにもないこと）が残っていない

---

## 5. 用語

| 用語 | 意味 |
|---|---|
| イシュー | この工程で決めないと次へ進めない論点。人が言葉にする |
| 決定ログ | 決定・理由・根拠の一覧。説明責任の根拠になる |
| 保留（Q） | 決められないと判断した論点。放置ではなく「決めない」と決めたもの |
| 受け入れ基準 | 成果物を受け取る条件。生成前に人が書く |
| 既成概念 | 要求にないのに「普通こうだ」で AI や人が足してしまうもの |
| 言語化 | 各工程で「何を AI に任せ、何を自分が決め、どう確かめたか」を書くこと |
